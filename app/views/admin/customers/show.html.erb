<div class="customer-details">
  <h2><%= @customer.first_name %> <%= @customer.last_name %>さん</h2>
  <table class="customer-details__list">
    <tr>
      <th class="customer-details__list--title">会員ID</th>
      <td class="customer-details__list--content"><%= @customer.id %></td>
    </tr>
    <tr>
      <th class="customer-details__list--title">氏名</th>
      <td class="customer-details__list--content"><%= @customer.first_name %> <%= @customer.last_name %></td>
    </tr>
    <tr>
      <th class="customer-details__list--title">フリガナ</th>
      <td class="customer-details__list--content"><%= @customer.first_name_kana %> <%= @customer.last_name_kana %></td>
    </tr>
    <tr>
      <th class="customer-details__list--title">郵便番号</th>
      <td class="customer-details__list--content"><%= @customer.postal_code %></td>
    </tr>
    <tr>
      <th class="customer-details__list--title">住所</th>
      <td class="customer-details__list--content"><%= @customer.address %></td>
    </tr>
    <tr>
      <th class="customer-details__list--title">電話番号</th>
      <td class="customer-details__list--content"><%= @customer.telephone_number %></td>
    </tr>
    <tr>
      <th class="customer-details__list--title">メールアドレス</th>
      <td class="customer-details__list--content"><%= @customer.email %></td>
    </tr>
    <tr>
      <th class="customer-details__list--title">ステータス</th>
      <td class="customer-details__list--content">
        <% if @customer.is_member %>
          有効
        <% else %>
          無効
        <% end %>
      </td>
    </tr>
  </table>
  <div class="customer-details__action">
    <div class="customer-details__action--item"><%= link_to "編集", edit_admin_customer_path(@customer), class: "customer-details__action--btn" %></div>
    <div class="customer-details__action--item">
      <!--注文履歴があればリンクの表示,ない場合はリンクなし-->
      <% if @customer.orders.present? %>
        <%= link_to "注文履歴一覧を見る", admin_order_path, class: "customer-details__action--btn" %>
      <% else %>
        <p class="customer-details__action--btn">注文履歴なし</p>
      <% end %>
    </div>
  </div>
</div>
