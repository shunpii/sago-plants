<div class="tp-item">
  <% @items.each do |item| %>
    <%= link_to item_path(item.id) do %>
      <div class="tp-item-list">
        <div>
          <% item.item_images.first(1).each do |image| %>
            <%= attachment_image_tag image, :image, size: "550x500",　format: 'jpeg' %>
          <% end %>
        </div>
        <div><%= item.name %></div>
        <div>
          <% if item.order_details.present? %>
            Sold Out!!
          <% else %>
            <%= item.price %>円
          <% end %>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
