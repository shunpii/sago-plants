<div>
  <% @items.each do |item| %>
    <%= link_to item_path(item.id) do %>
      <div><%= attachment_image_tag item, :image, size: "550x500",　format: 'jpeg' %></div>
      <div><%= item.name %></div>
      <div><%= item.price %>円</div>
    <% end %>
  <% end %>
</div>
